---
title: "Blog"
layout: "blogs"
description: "All the Blog Posts"
---

<div>
      {%- for page in collections.posts -%}
        <div>
          <a href="{{ page.url }}">{{ page.data.title }}</a>
          <p>{{ page.date.toLocaleDateString("en-GB") }}</p>
        </div>
        <hr class="blog_list"/>
      {%- endfor -%}
    </div>
